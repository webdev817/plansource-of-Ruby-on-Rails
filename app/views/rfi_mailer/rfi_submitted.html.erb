<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
        <h1><%= @subject %></h1>
        <p><strong>Job:</strong> <%= @rfi.job.name %></p>
        <p><strong>RFI Name:</strong> <%= @rfi.subject %></p>
        <p><strong>Submitter:</strong> <%= @rfi.user.first_name %> <%= @rfi.user.last_name %></p>
        <p><strong>Submitter Email:</strong> <%= @rfi.user.email %></p>
        <p><strong>Notes:</strong> <%= @rfi.notes %></p>

      <p><strong>Attachments:</strong></p>

        <% @rfi.attachments.each do |attachment| %>
          <p>
            <%= link_to attachment.filename, api_rfi_download_attachment_url(attachment.id, host: ENV["SITE_URL"]) %>
          </p>
        <% end %>

        <% if @rfi.attachments.empty? %>
          <p>No Attachments</p>
        <% end %>

        <br>

        <p>
          <%= link_to "View RFIs and ASIs for this job.", app_url(host: ENV["SITE_URL"]) + "#/jobs/#{@rfi.job.id}/rfi_asi" %>
        </p>
    </body>
</html>
